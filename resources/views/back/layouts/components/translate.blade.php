{{-- translations.super.welcome --}}
<script>
    const translations = {
        global: @json(trans('global')),
        login: @json(trans('login')),
        passwords: @json(trans('passwords')),
        register: @json(trans('register')),
        super: @json(trans('super')),
        verify_email: @json(trans('verify_email')),
    };
</script>
